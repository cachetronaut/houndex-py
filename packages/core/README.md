# houndex-core (Python)

Core contracts for a storage-decoupled, last-mile RAG framework. This is the
Python implementation; it mirrors the TypeScript `houndex/core` package and
is held to the same contracts by a shared cross-language parity fixture.

See the [repository README](../../README.md) for the project overview.
