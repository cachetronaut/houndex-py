# houndex-pipeline (Python)

Deterministic ingestion and enrichment transforms built on `houndex-core`:
chunking, content-hash dedupe, source-tier classification, deterministic
enrichment, and a dependency-injected ingestion orchestrator. Mirrors the
TypeScript `houndex/pipeline` package.

See the [repository README](../../README.md) for the project overview.
